server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NicolasOrtiz7/ms-social-network
          searchPaths: config-data
  application:
    name: config-service